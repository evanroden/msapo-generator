# Portability

The complete host, AI-provider, PDF-reader, converter, and storage migration guide is in [`docs/PORTABILITY.md`](docs/PORTABILITY.md).

Run `python -m app.doctor` in a candidate deployment before switching traffic.
