# Core
anthropic>=0.39.0
python-dotenv>=1.0.0

# Document generation
python-docx>=1.1.0
openpyxl>=3.1.5,<4.0.0
# Harden XML parsing used by workbook/document libraries.
defusedxml>=0.7.1

# PDF text extraction
PyMuPDF>=1.24.0

# Bounded image normalization (all supported formats -> vision-safe JPEG)
Pillow>=11.3.0
pillow-heif>=1.1.1

# Web UI. 1.61 adds the per-widget upload cap used to enforce Smartsheet's
# 30 MB attachment limit before an oversized quote is read or analyzed.
streamlit>=1.61.0,<2.0.0

# HTTP (for Gotenberg backend and optional integrations)
requests>=2.32.0

# Optional: Windows/macOS PDF conversion (requires MS Word installed)
# docx2pdf>=0.10
